# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'demo-bitrise' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for demo-bitrise
  pod 'Firebase/Analytics'
  pod 'Firebase/Core'

  target 'demo-bitriseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'demo-bitriseUITests' do
    # Pods for testing
  end

end
